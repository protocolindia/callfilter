<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:gravity="center" android:padding="24dp"
    android:background="@color/bg">
    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="🔒 Set PIN" android:textSize="28sp" android:textStyle="bold"
        android:textColor="@color/text"/>
    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="Pick a 4-digit PIN. You'll use this every time you open the app."
        android:textSize="13sp" android:textColor="@color/subtext"
        android:layout_marginTop="6dp" android:layout_marginBottom="32dp"
        android:gravity="center"/>
    <EditText android:id="@+id/pinInput"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:hint="PIN (4 digits)" android:textColor="@color/text"
        android:textColorHint="@color/muted" android:background="@drawable/input_bg"
        android:paddingStart="12dp" android:paddingEnd="12dp"
        android:inputType="numberPassword" android:maxLength="4"
        android:textSize="18sp" android:gravity="center" android:letterSpacing="0.4"
        android:layout_marginBottom="12dp"/>
    <EditText android:id="@+id/pinInput2"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:hint="Confirm PIN" android:textColor="@color/text"
        android:textColorHint="@color/muted" android:background="@drawable/input_bg"
        android:paddingStart="12dp" android:paddingEnd="12dp"
        android:inputType="numberPassword" android:maxLength="4"
        android:textSize="18sp" android:gravity="center" android:letterSpacing="0.4"
        android:layout_marginBottom="20dp"/>
    <Button android:id="@+id/btnSavePin"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:text="Save PIN" android:textStyle="bold" android:textSize="14sp"
        android:textColor="@color/white" android:background="@drawable/btn_accent"/>
</LinearLayout>
